import styled from 'styled-components';

const TestResultPage = () => {
    return <PrintContainer>프린트할 공간이에요</PrintContainer>;
};

export default TestResultPage;

const PrintContainer = styled.div`
    width: 50%;
    height: 50%;
    background: #ffffff;
    color: #070707;
`;
